import React, { Fragment } from "react";
import Auth from "../../Components/Auth/Auth";

const Registration = () => {
  return (
    <Fragment>
      <Auth />
    </Fragment>
  );
};

export default Registration;
